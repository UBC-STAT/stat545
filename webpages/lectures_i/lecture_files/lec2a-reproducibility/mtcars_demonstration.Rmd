---
title: "Report"
author: "Grace"
date: "2025-09-09"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
library(dplyr)
```

<!--- I need to finish this analysis ASAP!!! --->

```{r}
#remove the first row
mtcars <- mtcars[-1, ]
head(mtcars)
```


The following code is hidden but the output is shown. 

```{r, echo = F}
# this is code where the output is shown, but the code is hidden! 
select(mtcars, mpg)
```


## Summary Statistics

There are `r nrow(mtcars)` cars included in the analysis. The mean mpg across the `r nrow(mtcars)` cars is `r mean(mtcars$mpg)`.

## Examination of Car Weight vs MPG

```{r}
plot(x = mtcars$wt, y = mtcars$mpg)
```
